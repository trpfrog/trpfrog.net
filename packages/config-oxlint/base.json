{
  "$schema": "../../node_modules/oxlint/configuration_schema.json",
  "plugins": [
    "eslint",
    "unicorn",
    "typescript",
    "node",
    "vitest",
    "import",
    "promise",
    "jsdoc",
    "oxc"
  ],
  "jsPlugins": [
    {
      // TODO(oxlint-native): remove this JS plugin once Oxlint natively supports
      // `no-restricted-exports`, `no-restricted-properties`, and `no-unused-vars` rules.
      // Migration steps are documented in packages/config-oxlint/README.md.
      "name": "eslint-core",
      "specifier": "./js-plugins/eslint-core.cjs"
    },
    {
      "name": "unused-imports",
      "specifier": "eslint-plugin-unused-imports"
    }
  ],
  "ignorePatterns": [
    "out",
    "dist",
    "build",
    "coverage",
    ".wrangler"
  ],
  "rules": {
    "eslint/no-unused-vars": [
      "error",
      {
        "args": "all",
        "argsIgnorePattern": "^_",
        "caughtErrors": "all",
        "caughtErrorsIgnorePattern": "^_",
        "destructuredArrayIgnorePattern": "^_",
        "varsIgnorePattern": "^_",
        "ignoreRestSiblings": true
      }
    ],
    // TODO(oxlint-native): replace with native rule when available
    "unused-imports/no-unused-imports": "error",
    "node/no-process-env": [
      "error",
      {
        "allowedVariables": [
          "NODE_ENV"
        ]
      }
    ],
    // TODO(oxlint-native): replace with native rule when available:
    // `no-restricted-properties` (or `eslint/no-restricted-properties`).
    "eslint-core/no-restricted-properties": [
      "error",
      {
        "object": "v",
        "property": "parse",
        "message": "Use @trpfrog.net/utils's `validate` or `validateUnknown` instead."
      },
      {
        "object": "v",
        "property": "safeParse",
        "message": "Use @trpfrog.net/utils's `safeValidate` or `safeValidateUnknown` instead."
      }
    ],
    "eslint-core/no-restricted-syntax": [
      "warn",
      {
        "selector": "Literal[regex]",
        "message": "Prefer a regex helper such as arkregex instead of raw regex literals."
      },
      {
        "selector": "NewExpression[callee.name='RegExp']",
        "message": "Prefer a regex helper such as arkregex instead of constructing RegExp directly."
      }
    ],
    "typescript/no-restricted-types": [
      "error",
      {
        "types": {
          "v.InferInput": {
            "message": "Use `@trpfrog.net/utils`'s `InferSchemaInput` instead."
          },
          "v.InferOutput": {
            "message": "Use `@trpfrog.net/utils`'s `InferSchemaOutput` instead."
          }
        }
      }
    ]
  },
  "overrides": [
    {
      "files": [
        "**/*.{js,ts,mjs,cjs,jsx,tsx,mts,cts}"
      ],
      "rules": {
        // TODO(oxlint-native): replace with native rule when available:
        // `no-restricted-exports` (or `eslint/no-restricted-exports`).
        "eslint-core/no-restricted-exports": [
          "error",
          {
            "restrictDefaultExports": {
              "direct": true,
              "named": true,
              "defaultFrom": true,
              "namedFrom": true,
              "namespaceFrom": true
            }
          }
        ]
      }
    },
    {
      "files": [
        "**/*.config.{mjs,cjs,js,jsx,ts,mts,cts,tsx}"
      ],
      "rules": {
        "eslint-core/no-restricted-exports": "off",
        "node/no-process-env": "off"
      },
      "env": {
        "node": true
      }
    },
    {
      "files": [
        "**/vitest.{mjs,cjs,js,jsx,ts,mts,cts,tsx}",
        "**/vitest.*.{mjs,cjs,js,jsx,ts,mts,cts,tsx}"
      ],
      "rules": {
        "eslint-core/no-restricted-exports": "off"
      }
    },
    {
      "files": [
        ".storybook/**/*.{mjs,cjs,js,jsx,ts,mts,cts,tsx}"
      ],
      "rules": {
        "eslint-core/no-restricted-exports": "off"
      }
    }
  ]
}
