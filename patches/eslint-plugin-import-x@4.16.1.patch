diff --git a/lib/utils/export-map.js b/lib/utils/export-map.js
index 890f3e86..324525a7 100644
--- a/lib/utils/export-map.js
+++ b/lib/utils/export-map.js
@@ -728,13 +728,21 @@ export function recursivePatternCapture(pattern, callback) {
         default:
     }
 }
+function getContextParserPath(context) {
+    try {
+        return context.parserPath;
+    }
+    catch {
+        return undefined;
+    }
+}
 function childContext(path, context) {
-    const { settings, parserOptions, parserPath, languageOptions, cwd, filename, physicalFilename, } = context;
+    const { settings, parserOptions, languageOptions, cwd, filename, physicalFilename, } = context;
     return {
         cacheKey: makeContextCacheKey(context) + '\0' + path,
         settings,
         parserOptions,
-        parserPath,
+        parserPath: getContextParserPath(context),
         languageOptions,
         path,
         cwd,
@@ -743,7 +751,7 @@ function childContext(path, context) {
     };
 }
 export function makeContextCacheKey(context) {
-    const { settings, parserPath, parserOptions, languageOptions, cwd } = context;
+    const { settings, parserOptions, languageOptions, cwd } = context;
     let hash = cwd +
         '\0' +
         stableHash(settings) +
@@ -758,7 +766,7 @@ export function makeContextCacheKey(context) {
     }
     hash +=
         '\0' +
-            stableHash(parserPath ?? languageOptions?.parser?.meta ?? languageOptions?.parser);
+            stableHash(getContextParserPath(context) ?? languageOptions?.parser?.meta ?? languageOptions?.parser);
     return hash;
 }
 //# sourceMappingURL=export-map.js.map
diff --git a/lib/utils/parse.js b/lib/utils/parse.js
index 2e56b321..493e845b 100644
--- a/lib/utils/parse.js
+++ b/lib/utils/parse.js
@@ -105,6 +105,11 @@ function getParserPath(filepath, context) {
             }
         }
     }
-    return context.parserPath;
+    try {
+        return context.parserPath;
+    }
+    catch {
+        return undefined;
+    }
 }
 //# sourceMappingURL=parse.js.map
